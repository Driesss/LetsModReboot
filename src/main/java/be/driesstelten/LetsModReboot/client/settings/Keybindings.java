package be.driesstelten.LetsModReboot.client.settings;

import org.lwjgl.input.Keyboard;

import be.driesstelten.LetsModReboot.reference.Names;
import net.minecraft.client.settings.KeyBinding;

public class Keybindings {
	
	public static KeyBinding herp = new KeyBinding(Names.Keys.HERP, Keyboard.KEY_H, Names.Keys.CATEGORY);
	public static KeyBinding derp = new KeyBinding(Names.Keys.DERP, Keyboard.KEY_R, Names.Keys.CATEGORY);

}
